{
  "builds": [
    {
      "src": "CarZone/wsgi.py",
      "use": "@vercel/python",
      "config": {
        "maxLambdaSize": "15mb",
        "runtime": "python3.12",
        "buildCommand": "pip install -r requirements.txt && python manage.py makemigrations && python manage.py migrate && python manage.py create_default_superuser && python manage.py collectstatic"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/CarZone/wsgi.py"
    },
    {
      "src": "/static/(.*)",
      "dest": "/static/$1"
    }
  ]
}
